void printLine(int width) {
	int i;
	for (i = 0; i < width; i++) {
		printf("-");
	}
}

// Displays a menu with given options. Returns the index  of the selected option (0-indexed)
int createMenu(char option1[10], char option2[10], char option3[10]) {
	int currentOption = 0;
	printLine(50);
}

